<h1>Index of Books</h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <a href="/books/<%= book.id %>">
        <img class="thumb" src="<%= book.photo %>">
        <%= book.title %>
      </a>
    </li>
  <% end %>
</ul>